/target
Cargo.lock

# Nix build outputs and direnv's cached shell. flake.lock IS committed.
result
result-*
.direnv/

# The throwaway Postgres cluster `pg-start` creates in the dev shell.
/.pgdata

# macOS
.DS_Store
